We now have: - working interactive chessboard - legal moves - PGN/FEN support - move history Next, implement ONLY the following improvements and engine architecture. ======================================== PART 1 — MOVE HISTORY / REVIEW SYSTEM ======================================== Current problems: - Undo permanently deletes moves - No rewind/forward navigation - Move list grows infinitely and forces scrolling downward Requirements: 1. Separate: - current live game state - review position state 2. Add: - first move button - previous move button - next move button - last move button 3. Move review must: - preserve full PGN history - allow stepping through moves without deleting them - allow returning to live position instantly 4. Move history UI: - must use a scrollable fixed-height container - should NOT grow infinitely - auto-scroll to latest move ONLY during live play - should NOT force scrolling during review mode 5. Highlight: - current reviewed move - current board position 6. Keep architecture modular. Do NOT redesign the whole app. ======================================== PART 2 — UNIVERSAL BOT INTERFACE ======================================== Design and implement a SIMPLE engine/bot integration system. IMPORTANT: The goal is future compatibility with: - simple Python bots - JavaScript bots - future UCI adapters Do NOT implement Stockfish/UCI yet. ======================================== ENGINE API REQUIREMENTS ======================================== Create a unified engine interface. Every engine should: - receive a position - return exactly one move Create: - Engine interface/types - Engine manager - Match controller ======================================== PYTHON BOT SUPPORT ======================================== Implement support for loading SIMPLE Python bot files. The Python bot protocol MUST be extremely simple and stable. The app should: - launch the Python file as a subprocess - send ONE JSON line through stdin - receive ONE JSON line through stdout ======================================== STANDARD INPUT FORMAT ======================================== The app sends: { "fen": "current FEN string", "moves": ["e2e4", "e7e5"], "turn": "w", "time_ms": 1000 } ======================================== STANDARD OUTPUT FORMAT ======================================== The Python bot MUST respond with: { "bestmove": "g1f3" } Only UCI move format is allowed. ======================================== STRICT RULES FOR PYTHON BOTS ======================================== Python bots: - must be a SINGLE .py file - must read ONE line from stdin - must print ONE JSON line to stdout - must not require GUI - must not require networking - must not require extra processes - should work with standard Python 3 ======================================== EXAMPLE PYTHON BOT ======================================== Generate a minimal example bot: - reads JSON input - picks a random legal move - returns bestmove JSON ======================================== APP FEATURES ======================================== Add: - Human vs Human - Human vs Bot - Bot vs Bot Keep implementation minimal and modular. ======================================== IMPORTANT ======================================== - Do NOT redesign existing board architecture - Do NOT add authentication - Do NOT add databases - Do NOT add multiplayer - Do NOT overengineer - Keep everything simple and extensible